# Core (always required)
numpy>=1.24
scipy>=1.11
pandas>=2.0
matplotlib>=3.7

# Stage 1: ND2 ingestion
nd2>=0.10

# Stage 2: segmentation (lazy-imported; only needed for real segmentation, not --demo)
cellpose>=3.0

# Stage 3: tracking (lazy-imported; only needed for the FUCCI-4 population)
btrack>=0.6

# Stage 4/5: gating
scikit-learn>=1.3

# Stage 8: dimensionality reduction
umap-learn>=0.5
